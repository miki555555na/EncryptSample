# セキュリティパラメータ(n,p,χ)の定義
import numpy as np

n = 256  # 次元
p = 12289  # 素数
# 離散ガウス分布の標準偏差
sigma = 3.2 
# 誤差分布χの定義（離散ガウス分布を使用）
def chi():
    # 離散ガウス分布からサンプリングする関数
    # ここでは簡略化のため、正規分布からサンプリングして整数に丸める方法を使用
    return int(np.random.normal(0, sigma))